torch	        # The core deep learning framework (PyTorch).
transformers	# For loading models (DistilGPT2), tokenizers, and pipelines.
datasets	    # For efficient data loading and preprocessing.
accelerate	    # Essential for managing distributed training, mixed precision, and GPU device setup.
bitsandbytes	# Required for 8-bit quantization, critical for fitting models on 4GB VRAM.
peft	        # Parameter-Efficient Fine-Tuning library (needed for LoRA).
pyyaml	        # For reading the configuration files (.yaml).
streamlit	    # The framework for building the project dashboard.
pytest	        # The testing framework.
tensorboard